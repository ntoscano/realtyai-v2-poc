# Ralph Progress Log
Started: Wed Mar  4 16:37:23 PST 2026

## Codebase Patterns
- Port assignments: 54321=realty-ai, 54322=tictactoe, 54323=shift-marketplace, 54324=midi-matching
- App ports: 3003=shift-api, 3004=midi-api, UI ports: 2026=shift-ui, 2027=midi-ui
- Always add NestJS apps to root .prettierignore (root uses @trivago/prettier-plugin-sort-imports which fails on TS decorators)
- NestJS apps need a local .prettierrc.js (without the sort-imports plugin) so eslint's prettier/recommended can parse decorators
- Symptom-to-specialty mapping is in src/shared/symptom-specialty-map.ts — reuse in both QuestionnaireService and ClinicianService
- TypeORM DeepPartial<T> does not accept null — use undefined for optional nullable fields in repo.create()
- Include tsconfig.build.json from the start for NestJS apps (nest start --watch requires it)
- shift-api reference files are on ralph/shift-marketplace branch (not main), use `git show ralph/shift-marketplace:path` to view them
- .env.local is NOT committed via .gitignore — need to create manually or in scaffolding step
- Export interfaces used as return types from services — TS 5.9 requires named types in public methods to be exported (TS4053)
- Use innerJoinAndSelect (not leftJoinAndSelect) when using pessimistic_write lock — PostgreSQL FOR UPDATE cannot be applied to nullable side of outer join
- For pessimistic locking: DataSource.createQueryRunner() → connect → startTransaction → setLock('pessimistic_write') → commit/rollback → release in finally
- Cancellation must null out slotId AND set slot relation to null — TypeORM's .save() uses relation objects to infer FK values, so nulling only the FK field is insufficient
- Use leftJoinAndSelect for slot in list queries since cancelled appointments have slotId=null
---

## 2026-03-04 - US-001
- Scaffolded apps/midi-api/ with NestJS 10, TypeORM, PostgreSQL config
- Files created: package.json, tsconfig.json, tsconfig.build.json, nest-cli.json, docker-compose.yml, .env.example, .env.local, .eslintrc.js, src/main.ts, src/app.module.ts, src/config/postgres.ts, src/config/typeorm.ts, src/config/entities.ts, 4 placeholder modules
- Updated root .prettierignore to include apps/midi-api
- **Learnings for future iterations:**
  - shift-api source files exist on ralph/shift-marketplace branch, not on main — use git show to reference them
  - .env.local was created as part of scaffolding (same values as .env.example for local dev)
  - pnpm install auto-discovers the new workspace package via pnpm-workspace.yaml (apps/*)
---

## 2026-03-04 - US-002
- Created 8 TypeORM entities across 4 modules: Patient, Clinician, Specialty, ClinicianSpecialty, StateLicense, AvailabilitySlot, Appointment, HealthQuestionnaire
- Updated src/config/entities.ts to register all 8 entities
- Files created: patient.entity.ts, clinician.entity.ts, specialty.entity.ts, clinician-specialty.entity.ts, state-license.entity.ts, availability-slot.entity.ts, appointment.entity.ts, health-questionnaire.entity.ts
- **Learnings for future iterations:**
  - Follow shift-api entity patterns: @PrimaryGeneratedColumn('uuid'), explicit @JoinColumn({ name: 'snake_case_id' }), @Index at class level above @Entity
  - ClinicianSpecialty uses composite PK with @PrimaryColumn on both clinicianId and specialtyId
  - Use 'simple-array' TypeORM type for array columns (symptoms, careGoals, currentMedications) — stores as comma-separated string
  - Bidirectional relations: OneToMany on parent side, ManyToOne with @JoinColumn on child side
  - Unique constraints via @Unique decorator at class level
---

## 2026-03-04 - US-003
- Implemented questionnaire submission with symptom-to-specialty mapping
- Files created: src/shared/symptom-specialty-map.ts, src/modules/questionnaire/dto/create-questionnaire.dto.ts, src/modules/questionnaire/questionnaire.service.ts, src/modules/questionnaire/questionnaire.controller.ts, .prettierrc.js
- Files modified: src/modules/questionnaire/questionnaire.module.ts (added TypeOrmModule.forFeature, controller, service)
- **Learnings for future iterations:**
  - eslint with plugin:prettier/recommended picks up root .prettierrc.js even when app is in .prettierignore — fix by creating local .prettierrc.js without the sort-imports plugin
  - TypeORM Repository.create() has overloaded signatures; use an intermediate `DeepPartial<Entity>` variable to help TS resolve the correct overload
  - DeepPartial<T> maps nullable columns to `T | undefined`, not `T | null` — pass undefined (or omit) instead of null
  - Symptom-to-specialty mapping extracted to shared utility for reuse by ClinicianService in US-004
---

## 2026-03-04 - US-004
- Implemented clinician matching endpoint with weighted scoring algorithm
- Files created: src/modules/clinician/clinician.service.ts, src/modules/clinician/clinician.controller.ts
- Files modified: src/modules/clinician/clinician.module.ts (added TypeOrmModule.forFeature, controller, service)
- Endpoints: GET /api/clinicians/match, GET /api/clinicians/:id, GET /api/clinicians/:id/slots, GET /api/clinicians, GET /api/patients
- Match score = 0.5*(specialty overlap) + 0.2*(availability) + 0.2*(rating) + 0.1*(experience), top 10 returned
- **Learnings for future iterations:**
  - Export interfaces used as return types from services — TS 5.9 strict mode requires named types to be exported when used in public methods of exported classes (TS4053)
  - Use innerJoin (not leftJoin) for filtering joins (e.g., state licensure filter) — keeps unmatched clinicians out of results
  - innerJoinAndSelect for data you need in the result (e.g., specialties) — loads the relation data
  - max_available for score normalization uses Math.max(...results, 1) to avoid division by zero
  - date comparison for license expiration: compare against now.toISOString().split('T')[0] for date-only comparison
  - Also added listClinicians() for future clinician dashboard (US-006 notes mention needing GET /api/clinicians)
---

## 2026-03-04 - US-005
- Implemented appointment booking endpoint with pessimistic locking for concurrent-safe slot booking
- Files created: src/modules/appointment/dto/book-appointment.dto.ts, src/modules/appointment/appointment.service.ts, src/modules/appointment/appointment.controller.ts
- Files modified: src/modules/appointment/appointment.module.ts (added TypeOrmModule.forFeature, controller, service)
- Endpoint: POST /api/appointments — books a slot with pessimistic_write lock, validates slot availability, clinician-slot ownership, patient existence, and state licensure
- Concurrent booking: first request succeeds (201), second gets 409 conflict
- **Learnings for future iterations:**
  - Use innerJoinAndSelect (not leftJoinAndSelect) when applying pessimistic_write lock — PostgreSQL FOR UPDATE cannot be applied to the nullable side of an outer join
  - Use intermediate `DeepPartial<Entity>` variable for queryRunner.manager.create() — same overload resolution issue as Repository.create()
  - Cast DTO string fields to entity union types (e.g., `dto.visitType as VisitType`) before passing to create() — TS won't narrow string to union automatically
  - Licensure check within transaction ensures consistency — queries state_license for verified, non-expired license in patient's state
---

## 2026-03-04 - US-006
- Implemented appointment listing and status management endpoints
- Files created: src/modules/appointment/dto/update-appointment.dto.ts
- Files modified: src/modules/appointment/appointment.service.ts (added listForPatient, listForClinician, updateStatus), src/modules/appointment/appointment.controller.ts (added GET and PATCH endpoints)
- Endpoints: GET /api/appointments?patient_id=X or clinician_id=X, PATCH /api/appointments/:id
- Cancel flow uses QueryRunner transaction: updates appointment status + cancelledAt/cancelledBy, and frees the slot (isBooked=false) for rebooking
- Status transitions: only 'scheduled' appointments can be updated (→cancelled, →completed, →no_show). Invalid transitions return 400.
- **Learnings for future iterations:**
  - Use ValidateIf decorator for conditional validation — e.g., cancelledBy is required only when status is 'cancelled'
  - Consolidate duplicate TypeORM entity imports into a single import statement — eslint --fix may split them but then you get duplicate imports
  - GET /api/clinicians (all active clinicians) was already added in US-004 — no changes needed to clinician controller for US-006
---

## 2026-03-04 - US-007
- Created src/cli/seed.ts with seed data for all 6 tables
- Seed order: specialties → clinicians → clinician-specialty associations → state licenses → patients → availability slots
- 6 specialties, 5 clinicians, state licenses (all verified, exp 2027), 4 patients, 8 slots/clinician over 7 days
- Uses upsert pattern: findOneBy + create/update to avoid duplicates on re-run
- Specialties use createQueryBuilder().insert().orUpdate() for true upsert
- Files created: src/cli/seed.ts
- **Learnings for future iterations:**
  - Clinician entity has no email/unique field — upsert by firstName+lastName combo
  - TypeORM `orUpdate()` takes snake_case column names (as in DB), not camelCase entity property names
  - `generatedMaps` from insert().returning('*') may be empty on conflict/update — always fall back to findOneBy
  - `seed` script was already defined in package.json from US-001 scaffolding
  - Availability slots use tomorrow through +7 days to ensure they're always in the future
---

## 2026-03-04 - US-008
- Verified full backend via curl tests: all endpoints working end-to-end
- Found and fixed critical bug: cancellation didn't release the unique slot_id constraint, blocking rebooking
- Fix: made slotId nullable on Appointment entity, null out slotId + slot relation on cancel, use leftJoinAndSelect for slot in list queries
- Files modified: src/modules/appointment/appointment.service.ts (cancel nulls slotId, list queries use leftJoin for slot, optional chaining on slot times), src/modules/appointment/entities/appointment.entity.ts (slotId nullable)
- All acceptance criteria verified:
  - GET /api/patients: 4 patients with state info
  - POST /api/questionnaires: 201 with matchedSpecialties
  - GET /api/clinicians/match: ranked clinicians filtered by state licensure
  - GET /api/clinicians/:id/slots: unbooked time slots
  - POST /api/appointments: 201 scheduled
  - Double-booking: 409 conflict
  - Unlicensed clinician: 400 with licensure error
  - GET /api/appointments?patient_id=X: appointment list
  - PATCH cancel: 200, slot freed, rebook succeeds
  - Invalid POST: 400 with descriptive validation errors
- **Learnings for future iterations:**
  - OneToOne with unique constraint on FK means cancelled appointments block rebooking — must null the FK on cancel
  - TypeORM .save() uses loaded relation objects to infer FK values — null both the FK field AND the relation property
  - Use leftJoinAndSelect instead of innerJoinAndSelect for nullable FK relations in list queries
  - Clean dist/ when restarting nest start --watch after entity changes — old compiled JS may persist
---

## 2026-03-04 - US-009
- Scaffolded apps/midi-ui/ with Next.js 14, React 18, Tailwind CSS, shadcn/ui components
- Files created: package.json, tsconfig.json, next.config.js, postcss.config.js, tailwind.config.ts, .env.example, .env.local, src/app/globals.css, src/lib/utils.ts, src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/badge.tsx, src/types/index.ts, src/lib/api/midiApi.ts, src/app/layout.tsx, src/app/page.tsx, src/app/questionnaire/page.tsx, src/app/clinician/page.tsx
- Updated root .prettierignore to include apps/midi-ui
- Types cover all domain entities: Patient, Clinician, Specialty, ClinicianSpecialty, StateLicense, AvailabilitySlot, Appointment, HealthQuestionnaire, MatchedClinician, plus type literals for Symptom, CareGoal, Severity, MenopauseStage, VisitType, AppointmentStatus, ClinicianCredential, CancelledBy
- API client (midiApi.ts) wraps all backend endpoints: listPatients, submitQuestionnaire, matchClinicians, getClinicianDetail, getClinicianSlots, bookAppointment, listAppointments, cancelAppointment, listClinicians
- **Learnings for future iterations:**
  - shift-ui patterns work well as template — same package.json deps, tsconfig, tailwind config, shadcn components
  - pnpm-workspace.yaml already includes apps/* glob so new apps are auto-discovered
  - Frontend types should mirror backend entity shapes but use string for Date/timestamp fields (JSON serialization)
  - QuestionnaireResponse extends HealthQuestionnaire with matchedSpecialties (computed, not stored)
  - ApiError interface pattern: extend Error with status field, cast in apiFetch catch block
---

## 2026-03-04 - US-010
- Implemented patient questionnaire and clinician matching page
- Two-phase page: questionnaire form → matched clinician results
- Files modified: src/app/questionnaire/page.tsx (full implementation replacing placeholder)
- Features implemented:
  - Patient selector dropdown (populated from GET /api/patients, shows name + state)
  - Symptom checkboxes with human-readable labels (7 options)
  - Severity radio buttons (mild/moderate/severe)
  - Care goals checkboxes (4 options)
  - Menopause stage dropdown (optional)
  - Additional notes textarea (optional)
  - Submit calls POST /api/questionnaires then GET /api/clinicians/match
  - Matched clinician cards ordered by match score: name, credential, specialties (matching highlighted with default Badge variant), match score %, rating, years experience, available slot count, next available date
  - "View Available Times" links to /book/[clinicianId]?patient_id=X&questionnaire_id=Y
  - Loading and error states handled
  - "New Questionnaire" button to start over
- **Learnings for future iterations:**
  - CSS `has-[:checked]` pseudo-class works well for styling checkbox/radio containers without JS state
  - Use native HTML selects per project convention (no shadcn Select)
  - Badge variant="default" for matching specialties, variant="secondary" for non-matching — provides visual distinction
  - Link wrapping Button component works for navigation from card footers
---

## 2026-03-04 - US-011
- Implemented appointment booking page at src/app/book/[clinicianId]/page.tsx
- Three-phase page: loading → slot selection → booking confirmation
- Features implemented:
  - Reads patient_id and questionnaire_id from URL search params (useSearchParams)
  - Clinician info card at top: name, credential, specialties (from clinicianSpecialties), bio, rating, years experience
  - Available time slots fetched from GET /api/clinicians/:id/slots
  - Slots grouped by date using Map, formatted with toLocaleDateString/toLocaleTimeString
  - Each slot rendered as an outline Button with start–end time range
  - Clicking a slot calls POST /api/appointments with all required fields
  - On success: shows confirmation card with clinician name, date, time, status badge, visit type
  - On 409: shows "This time slot is no longer available" error and auto-refreshes slot list
  - On other errors: shows error message inline
  - Loading and error states handled throughout
  - Back to Questionnaire link on confirmation
- Files created: src/app/book/[clinicianId]/page.tsx
- **Learnings for future iterations:**
  - useParams() returns string for dynamic route segments — cast with `as string`
  - Grouping slots by date: Map preserves insertion order, so pre-sorted slots stay sorted within groups
  - eslint --fix resolves all prettier formatting — run it before commit to avoid lint issues
  - ApiError type cast pattern: `const apiErr = err as ApiError; if (apiErr.status === 409)` for typed error handling
---
