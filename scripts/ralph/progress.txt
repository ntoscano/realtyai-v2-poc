# Ralph Progress Log
Started: Thu Feb 26 16:55:08 PST 2026

## Codebase Patterns
- NestJS apps need `root: true` in `.eslintrc.js` and must be added to root `.prettierignore` and `.eslintrc.js` ignorePatterns (root config doesn't support decorators)
- `.env.local` is gitignored by default - use `.env.example` for committed env template
- TypeORM config uses `SnakeNamingStrategy` for PostgreSQL column naming conventions
- Use `dotenv.config({ path: path.join(__dirname, '../../.env.local') })` in CLI scripts and `'../.env.local'` in main.ts
- Entity registry pattern: centralize all entities in `src/config/entities.ts`
- PostGraphile auto-converts snake_case to camelCase in GraphQL (board_state -> boardState)
- PostGraphile mutation input types: `CreateGameInput` has `game: {}` wrapper, `UpdateGameByIdInput` has `id` + `gamePatch: {...}`
- JSONB columns accept JSON variables directly in PostGraphile mutations (use GraphQL variables, not inline objects)
- Use `app.enableCors()` in NestJS main.ts for cross-origin frontend requests
- Frontend port assignments: realty-ai=2024, tictactoe=2025; Backend: realty-ai-api=3001, tictactoe-api=3002
- Next.js frontend apps need at least `src/app/page.tsx` and `src/app/layout.tsx` for typecheck to work

---

## 2026-02-26 - US-001
- What was implemented: Backend project scaffolding for tictactoe-api
- Files changed:
  - apps/tictactoe-api/package.json (NestJS deps, scripts)
  - apps/tictactoe-api/tsconfig.json, tsconfig.build.json, nest-cli.json
  - apps/tictactoe-api/.eslintrc.js (root: true for decorator support)
  - apps/tictactoe-api/docker-compose.yml (postgres:16 on port 54322)
  - apps/tictactoe-api/.env.local, .env.example
  - apps/tictactoe-api/src/config/entities.ts (empty array placeholder)
  - Root .prettierignore, .eslintrc.js (added tictactoe-api to ignore)
- **Learnings for future iterations:**
  - .env.local is gitignored - don't try to `git add` it
  - pnpm install succeeds with peer dep warnings for postgraphile-nest (expects NestJS ^7 but we use ^8) - this is fine, same as realty-ai-api
  - Need at least one .ts file in src/ for typecheck to work (created entities.ts early)
---

## 2026-02-26 - US-002
- What was implemented: Database configuration utilities for TypeORM and PostGraphile
- Files changed:
  - apps/tictactoe-api/src/config/postgres.ts (getConfig, getAdminConfig, getClient with port 54322 and db 'tictactoe')
  - apps/tictactoe-api/src/config/typeorm.ts (getTypeOrmConfig, getTypeOrmModuleConfig with SnakeNamingStrategy, default DataSource export for CLI)
  - apps/tictactoe-api/src/config/graphile.ts (postgraphileConfigFactory for PostGraphile NestJS integration)
- **Learnings for future iterations:**
  - entities.ts was already created in US-001 scaffolding - no need to recreate it
  - typeorm.ts dotenv.config() path uses `__dirname + '../../.env.local'` since the file is in src/config/
  - graphile.ts is identical to realty-ai-api version - no project-specific changes needed
---

## 2026-02-26 - US-003
- What was implemented: Game entity with TypeORM decorators and type exports
- Files changed:
  - apps/tictactoe-api/src/modules/game/game.entity.ts (Game entity with id, boardState, status, winner, moves, createdAt, updatedAt; CellValue, GameMove, GameStatus type exports)
  - apps/tictactoe-api/src/config/entities.ts (added Game to entity registry)
- **Learnings for future iterations:**
  - JSONB column defaults must be plain JS values (arrays/objects), TypeORM handles serialization
  - SnakeNamingStrategy auto-converts camelCase property names to snake_case column names, but explicit `name` in @Column/@CreateDateColumn is still best practice for clarity
  - Game module directory was pre-created in US-001 scaffolding as empty dir
---

## 2026-02-26 - US-004
- What was implemented: NestJS application bootstrap (app.service, app.controller, app.module, main.ts)
- Files changed:
  - apps/tictactoe-api/src/app.service.ts (getRoot returns {message: 'TicTacToe API'}, getHealth returns {status: 'ok'})
  - apps/tictactoe-api/src/app.controller.ts (GET / and GET /health endpoints)
  - apps/tictactoe-api/src/app.module.ts (TypeOrmModule.forRoot + PostGraphileModule.forRootAsync)
  - apps/tictactoe-api/src/main.ts (bootstrap on port 3002 with dotenv from .env.local)
- **Learnings for future iterations:**
  - Files were already committed in prior iteration but PRD wasn't updated - always update PRD after commit
  - main.ts dotenv path uses `path.join(__dirname, '../.env.local')` (relative to dist/ at runtime)
  - PostGraphileModule.forRootAsync uses empty imports/inject arrays with postgraphileConfigFactory
---

## 2026-02-26 - US-005
- What was implemented: Database migration and seed data (retroactively marking as passed - was committed prior)
- Files changed:
  - apps/tictactoe-api/src/migrations/1772154510409-CreateGame.ts (TypeORM migration for game table)
  - apps/tictactoe-api/src/cli/seed.ts (inserts 2 sample games)
  - apps/tictactoe-api/src/cli/migration.ts (cleanup utility placeholder)
- **Learnings for future iterations:**
  - Migration filename includes timestamp prefix for ordering
  - Seed script and migration were committed together in same story
---

## 2026-02-26 - US-006
- What was implemented: Verified PostGraphile GraphQL API and enabled CORS
- Files changed:
  - apps/tictactoe-api/src/main.ts (added `app.enableCors()`)
- **Learnings for future iterations:**
  - PostGraphile generates allGames { nodes { ... } } connection pattern automatically
  - createGame mutation accepts `input: { game: {} }` with empty game object for defaults
  - updateGameById mutation uses `input: { id: UUID, gamePatch: { ... } }` pattern
  - JSONB fields (boardState, moves) accept JSON directly via GraphQL variables (not inline)
  - GraphQL field names are auto-camelCased from snake_case DB columns
---

## 2026-02-26 - US-007
- What was implemented: Frontend project scaffolding for tictactoe Next.js app
- Files changed:
  - apps/tictactoe/package.json (Next.js 14, React 18, Tailwind, LangChain, Apollo Client, shadcn/ui deps)
  - apps/tictactoe/tsconfig.json (extending @boilertowns-example/tsconfig/web.json with @/* path alias)
  - apps/tictactoe/next.config.js, postcss.config.js, tailwind.config.ts (copied from realty-ai)
  - apps/tictactoe/.env.local (Bedrock creds + NEXT_PUBLIC_GRAPHQL_URL)
  - apps/tictactoe/.env.example (template without secrets)
  - apps/tictactoe/src/app/page.tsx, layout.tsx (minimal placeholders for typecheck)
  - pnpm-lock.yaml (updated with new workspace package)
- **Learnings for future iterations:**
  - .env.local is gitignored - won't be committed (same as backend pattern)
  - Need at least page.tsx and layout.tsx in src/app/ for Next.js and typecheck to work
  - Dev runs on port 2025 (realty-ai uses 2024, tictactoe-api uses 3002)
  - Removed @aws-sdk/client-bedrock-runtime, @langchain/community, promptfoo, weather deps from realty-ai deps
  - Peer dep warnings are expected and non-blocking (same as other workspace packages)
---

## 2026-02-26 - US-008
- What was implemented: Frontend base files (globals.css, layout.tsx, utils.ts) and shadcn/ui components (button, card, badge, scroll-area)
- Files changed:
  - apps/tictactoe/src/app/globals.css (Tailwind base + CSS variables for light/dark themes)
  - apps/tictactoe/src/app/layout.tsx (updated with Metadata type, globals.css import, Tailwind body classes)
  - apps/tictactoe/src/lib/utils.ts (cn() utility using clsx + tailwind-merge)
  - apps/tictactoe/src/components/ui/button.tsx (shadcn/ui Button with variants)
  - apps/tictactoe/src/components/ui/card.tsx (shadcn/ui Card components)
  - apps/tictactoe/src/components/ui/badge.tsx (shadcn/ui Badge with variants)
  - apps/tictactoe/src/components/ui/scroll-area.tsx (shadcn/ui ScrollArea)
- **Learnings for future iterations:**
  - shadcn/ui components are self-contained and can be directly copied between Next.js apps without modification
  - All UI components use @/lib/utils cn() function - must create utils.ts before UI components
  - globals.css CSS variables define the shadcn/ui theme - required for component styles to work
  - layout.tsx needs `import './globals.css'` for styles to be applied
---

## 2026-02-26 - US-009
- What was implemented: Game types (CellValue, Board, GameMove, GameStatus, GameState, GameSummary) and deterministic game logic functions
- Files changed:
  - apps/tictactoe/src/types/game.ts (all game type definitions)
  - apps/tictactoe/src/lib/game/gameLogic.ts (WIN_LINES, checkWinner, isBoardFull, getAvailablePositions, getGameStatus, getCurrentTurn, isValidMove, applyMove, createEmptyBoard, formatBoardForDisplay)
- **Learnings for future iterations:**
  - Board type is a 9-element tuple, not an array - use `as Board` when spreading
  - All game logic functions are pure - no state mutations, always return new values
  - getCurrentTurn counts X's and O's to determine turn (X goes first, so X count <= O count means X's turn)
  - formatBoardForDisplay uses '.' for empty cells - this format is used in the LLM prompt
  - Linter reformats to tabs (project uses tabs, not spaces)
---
