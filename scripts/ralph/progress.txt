## Codebase Patterns
- Use SnakeNamingStrategy: camelCase in TypeScript -> snake_case in DB columns (automatic via TypeORM config)
- Migrations are in `apps/tictactoe-api/src/migrations/` with timestamp prefix
- Run migrations with `pnpm typeorm:migration:run` from `apps/tictactoe-api/`
- PostgreSQL on port 54322 (not default 5432) to avoid conflicts
- Game entity at `apps/tictactoe-api/src/modules/game/game.entity.ts`
- Types (CellValue, GameMove, GameStatus, GameMode) are defined in the entity file AND in `apps/tictactoe-api/src/lib/game/types.ts`
- Pure game logic functions in `apps/tictactoe-api/src/lib/game/gameLogic.ts` — import types from `./types`
- Typecheck: `pnpm typecheck` from `apps/tictactoe-api/`
- Pre-commit hooks run eslint and prettier via lint-staged
- DTOs in `apps/tictactoe-api/src/modules/game/dto/` — use class-validator for validation
- GameService has `toGameStateDto()` private method to map entity -> DTO (does NOT expose player tokens)
- GameController uses `@Controller('api/games')` prefix
- Use `@UsePipes(new ValidationPipe({ whitelist: true }))` per-endpoint for input validation
- For transactional operations with row locking: inject `DataSource`, create queryRunner, use `setLock('pessimistic_write')` for SELECT FOR UPDATE
- Board is JSONB in DB — cast to `Board` type from `../../lib/game/types` when passing to game logic functions
- AI module at `apps/tictactoe-api/src/modules/ai/` — AiService.generateMove(board) invokes LangGraph pipeline
- AI files use kebab-case filenames: move-graph.ts, graph-state.ts, move-validator.ts, move-prompt.ts, llm.ts, nodes.ts
- NestJS upgraded to v10, TypeScript upgraded to v5.9 — required for compatibility with zod v4 (transitive dep from @langchain/core)
- AWS Bedrock env vars: AI_AWS_BEDROCK_ACCESS_KEY_ID, AI_AWS_BEDROCK_SECRET_ACCESS_KEY, AI_AWS_BEDROCK_REGION

# Ralph Progress Log
Started: Fri Feb 27 15:47:49 PST 2026
---

## 2026-02-27 16:00 - US-001
- Implemented database schema migration for game mode and turn tracking
- Files changed:
  - `apps/tictactoe-api/src/modules/game/game.entity.ts` - Added mode, currentTurn, playerXToken, playerOToken columns with types
  - `apps/tictactoe-api/src/migrations/1772236312254-AddGameModeAndTurnTracking.ts` - New migration: truncates game data, adds 4 new columns
- **Learnings for future iterations:**
  - SnakeNamingStrategy handles camelCase -> snake_case automatically, but explicit `name` overrides in `@Column` decorators take precedence (e.g., `board_state`)
  - Migration ran successfully with TRUNCATE before ALTER TABLE to avoid default value issues on existing rows
  - Typecheck and lint pass cleanly
---

## 2026-02-27 16:15 - US-002
- Copied game logic and shared types to backend
- Files changed:
  - `apps/tictactoe-api/src/lib/game/types.ts` - Created with CellValue, Board (9-element tuple), GameMove, GameStatus, GameMode types
  - `apps/tictactoe-api/src/lib/game/gameLogic.ts` - Copied verbatim from frontend, updated import from `@/types/game` to `./types`
- **Learnings for future iterations:**
  - The frontend types file at `apps/tictactoe/src/types/game.ts` has extra types (GameState, GameSummary) not needed in backend — only copied the shared subset
  - The entity file already defines CellValue, GameMove, GameStatus, GameMode — there is now duplication between entity types and lib/game/types.ts. Future stories may want to consolidate by importing from lib/game/types.ts in the entity
  - All 9 pure functions are available: checkWinner, isBoardFull, getAvailablePositions, getGameStatus, getCurrentTurn, isValidMove, applyMove, createEmptyBoard, formatBoardForDisplay
---

## 2026-02-27 16:35 - US-003
- Created GameModule with POST /api/games endpoint for creating games
- Files changed:
  - `apps/tictactoe-api/src/modules/game/dto/create-game.dto.ts` - CreateGameDto with @IsIn validation for mode field
  - `apps/tictactoe-api/src/modules/game/dto/game-state.dto.ts` - GameStateDto and CreateGameResponseDto (game state + playerToken)
  - `apps/tictactoe-api/src/modules/game/game.service.ts` - GameService with createGame() and toGameStateDto() helper
  - `apps/tictactoe-api/src/modules/game/game.controller.ts` - GameController with POST /api/games
  - `apps/tictactoe-api/src/modules/game/game.module.ts` - GameModule importing TypeOrmModule.forFeature([Game])
  - `apps/tictactoe-api/src/app.module.ts` - Registered GameModule in AppModule imports
  - `apps/tictactoe-api/package.json` - Added class-validator and class-transformer dependencies
- **Learnings for future iterations:**
  - class-validator and class-transformer were already listed in package.json but not installed — pnpm install pulled them in
  - Entity types (CellValue, GameMove, GameStatus, GameMode) are imported from game.entity.ts in DTOs — keeps a single source of truth within the game module
  - GameStateDto maps boardState -> board to match the frontend's expected API shape
  - playerToken is returned as a separate top-level field in CreateGameResponseDto, not part of GameStateDto
  - ValidationPipe with whitelist:true strips unknown properties from request body
---

## 2026-02-27 17:00 - US-004
- Added GET /api/games/:id endpoint to fetch game state
- Files changed:
  - `apps/tictactoe-api/src/modules/game/game.service.ts` - Added getGame() method with NotFoundException for missing games
  - `apps/tictactoe-api/src/modules/game/game.controller.ts` - Added @Get(':id') endpoint returning GameStateDto
- **Learnings for future iterations:**
  - toGameStateDto() already strips playerXToken and playerOToken — no extra work needed for security
  - NotFoundException from @nestjs/common auto-returns 404 with descriptive JSON message
  - Pre-commit eslint decorator parsing errors are pre-existing and not caused by changes — lint-staged still passes
---

## 2026-02-27 17:20 - US-005
- Added POST /api/games/:id/move endpoint for making moves with full game logic validation
- Files changed:
  - `apps/tictactoe-api/src/modules/game/dto/make-move.dto.ts` - Created MakeMoveDto with @IsInt, @Min(0), @Max(8) validation on position
  - `apps/tictactoe-api/src/modules/game/game.service.ts` - Added makeMove() method with transaction, SELECT FOR UPDATE, game logic validation
  - `apps/tictactoe-api/src/modules/game/game.controller.ts` - Added @Post(':id/move') endpoint with ValidationPipe
- **Learnings for future iterations:**
  - TypeORM `DataSource` is injectable without @Inject decorator — NestJS auto-resolves it from the TypeORM module
  - Use `queryRunner.manager.getRepository(Game).createQueryBuilder('game').setLock('pessimistic_write')` for SELECT ... FOR UPDATE
  - Board stored as JSONB in DB needs `as Board` cast when using with game logic functions expecting the 9-element tuple type
  - Game logic functions imported from `../../lib/game/gameLogic` — relative path from modules/game/ directory
  - `moveNumber` is 1-indexed: `game.moves.length + 1` before adding the new move
  - Winner field on game over: set to 'X' or 'O' for wins, null for draws; currentTurn only flips on in_progress continuation
---

## 2026-02-27 17:40 - US-006
- Added GET /api/games endpoint to list recent games with optional limit
- Files changed:
  - `apps/tictactoe-api/src/modules/game/game.service.ts` - Added listGames(limit) method using repository.find with order and take
  - `apps/tictactoe-api/src/modules/game/game.controller.ts` - Added @Get() handler with @Query('limit') param, placed before @Get(':id') to avoid route conflicts
- **Learnings for future iterations:**
  - Route order matters in NestJS controllers: @Get() must come before @Get(':id') or the parameterized route will catch all requests
  - Query params come as strings — parse to int before passing to service, with fallback to undefined for default
  - Lint auto-fixed `limit: number = 20` to `limit = 20` (type inference from default value)
  - toGameStateDto() already strips player tokens so listGames response is safe by default
---

## 2026-02-27 18:00 - US-007
- Added POST /api/games/:id/join endpoint for Player O to join PvP games
- Files changed:
  - `apps/tictactoe-api/src/modules/game/game.service.ts` - Added joinGame() method with mode validation, conflict check, and playerOToken generation
  - `apps/tictactoe-api/src/modules/game/game.controller.ts` - Added @Post(':id/join') endpoint returning CreateGameResponseDto
- **Learnings for future iterations:**
  - ConflictException from @nestjs/common auto-returns 409 with descriptive JSON message — same pattern as NotFoundException for 404
  - joinGame reuses CreateGameResponseDto (game state + playerToken) — same response shape as createGame since both return a token to the caller
  - No ValidationPipe needed on join endpoint since there's no request body to validate
  - Route placed before :id/move to maintain clean route ordering (though NestJS resolves literal segments before params so it works either way)
---

## 2026-02-27 18:30 - US-008
- Migrated AI pipeline to NestJS backend
- Files changed:
  - `apps/tictactoe-api/src/modules/ai/ai.module.ts` - Created AiModule exporting AiService
  - `apps/tictactoe-api/src/modules/ai/ai.service.ts` - Created AiService with generateMove(board) method, defense-in-depth isValidMove check
  - `apps/tictactoe-api/src/modules/ai/move-graph.ts` - Copied from frontend, updated imports to relative paths
  - `apps/tictactoe-api/src/modules/ai/nodes.ts` - Copied from frontend, updated imports: `../../lib/game/gameLogic` and local AI modules
  - `apps/tictactoe-api/src/modules/ai/graph-state.ts` - Copied from frontend, updated Board import to `../../lib/game/types`
  - `apps/tictactoe-api/src/modules/ai/move-validator.ts` - Copied verbatim (no import changes needed, only uses zod)
  - `apps/tictactoe-api/src/modules/ai/llm.ts` - Copied verbatim (uses @langchain/aws, env vars for credentials)
  - `apps/tictactoe-api/src/modules/ai/move-prompt.ts` - Copied verbatim (uses @langchain/core/prompts)
  - `apps/tictactoe-api/src/modules/game/game.module.ts` - Added AiModule to imports
  - `apps/tictactoe-api/package.json` - Added @langchain/aws, @langchain/core, @langchain/langgraph, zod; upgraded NestJS to v10, TypeScript to v5.9
  - `apps/tictactoe-api/.env.local` - Added AI_AWS_BEDROCK_* env vars
  - `apps/tictactoe-api/.env.example` - Added AI_AWS_BEDROCK_* env vars
- **Learnings for future iterations:**
  - Zod v4 (4.3.6) is pulled in as a transitive dep from @langchain/core — its .d.cts files are incompatible with TypeScript 4.9 (skipLibCheck doesn't skip .d.cts). Must use TypeScript 5.x.
  - NestJS 8's @Inject/@InjectRepository parameter decorator types are incompatible with TypeScript 5.x (TS1239 error). Upgrading to NestJS 10 fixes this.
  - The NestJS v8 -> v10 + TS 4.9 -> 5.9 upgrade was necessary for the langchain dependency chain: @langchain/core -> zod v4 -> requires TS 5.x -> requires NestJS 10
  - AI files use kebab-case filenames in backend (move-graph.ts vs moveGraph.ts in frontend) to match backend conventions
  - zod@3.25.32 is installed as direct dependency for the move-validator.ts; zod@4.3.6 exists as transitive from @langchain/core
  - AiService wraps the LangGraph pipeline with a defense-in-depth isValidMove() check after AI generates a move
---
